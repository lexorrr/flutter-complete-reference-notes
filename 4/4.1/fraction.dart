// You can also use the library keyword to "name"
// your library as you prefer. This keyword really
// just names the library as you like, nothing more,
// and it's not required.
library super_duper_fraction;

class Fraction {
  final int numerator;
  final int denominator;

  const Fraction(this.numerator, this.denominator);
}